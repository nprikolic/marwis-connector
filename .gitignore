# Python
__pycache__/
*.pyc

# PyInstaller build output (the exe is built by CI and attached to Releases)
build/
dist/
*.exe

# Data / logged output (all generated; regenerated by the scripts)
*.sqlite
*.sqlite-journal
data/*.csv
data/*.png

# Scratch working dir: raw runs + phone GPS tracks fed to merge_gps.py
merge/

# Scratch / redirected console output
out.txt
err.txt

# Saved marketing web page — tracking-script junk; the PDFs and extracted/ text
# dumps are the real reference, so this big asset folder stays untracked.
docs/reference/web/

# Android / Gradle
android/.gradle/
android/build/
android/app/build/
android/.idea/
android/local.properties
android/captures/
*.apk
*.aab
*.keystore

# OS / editor
.DS_Store
Thumbs.db
